#ifndef SUPERFICIE_H
#define SUPERFICIE_H

#include <sstream>
#include <fstream>
#include <string>
#include "errores.h"

using namespace std;

class Superficie{

protected:
    // Atributos
    bool transitable;
    bool construible;
    bool accesible;
    string color; // "ROJO" o "R" ? 

public:
    // Metodos


};


#endif // SUPERFICIE_H